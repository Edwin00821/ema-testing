export default interface IPersonalData {
  id_per: number;
  nombre_per: string;
  appat_per: string;
  apmat_per: string;
  fecha_de_nacimiento_per: Date;
  id_gen: number;
  id_int: number;
  valida_per: number;
}
