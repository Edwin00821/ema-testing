export default interface IRegisterAdmin {
  id_per: string;
  nombre_per: string;
  appat_per: string;
  apmat_per: string;
  edad_per: number;
  tel_per: string;
  correo_per: string;
  pass_per: string;
  id_rol: number;
  id_int: number;
  id_gen: number;
  valida_per: number;
}
